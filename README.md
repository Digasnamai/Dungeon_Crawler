This project features a randomly generated 3D dungeon with first person mobility, ready to be customised with textures.
The script utilizes recursive backtracking to generate a grid-based dungeon with a randomized critical path, branching corridors, and room types. 
Once the layout is finalized, it instantiates 3D room scenes, spawning hallways and walls based on adjacent cells.
